/** TODO
 * create model on front and back end for request
 * create model on front and back end for request for the response
 * create get request function on backend
 * call request function from frontend to send keywords
 */
export class KeywordsModel {
  keyword: String;
  keywordTwo: String;

  constructor (keyword: String, keywordTwo: String) {
    this.keyword = keyword;
    this.keywordTwo = keywordTwo;
  }}
